In order to download and install the libraries you need

Requirements:
Microsoft Visual Studio 2015 (or newer) with C/C++ Compiler installed.
Python3 (Python3.5 and above)
CMake for windows and add it to your system environment variables.

Then run requirements.txt to download and install the required libraries and then main.py